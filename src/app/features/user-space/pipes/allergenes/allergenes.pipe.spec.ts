import { AllergenesPipe } from './allergenes.pipe';

describe('AllergenesPipe', () => {
  it('create an instance', () => {
    const pipe = new AllergenesPipe();
    expect(pipe).toBeTruthy();
  });
});
