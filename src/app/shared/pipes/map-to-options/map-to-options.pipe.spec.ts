import { MapToOptionsPipe } from './map-to-options.pipe';

describe('MapToOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new MapToOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
