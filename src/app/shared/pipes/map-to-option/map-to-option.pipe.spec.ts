import { MapToOptionPipe } from './map-to-option.pipe';

describe('MapToOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new MapToOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
